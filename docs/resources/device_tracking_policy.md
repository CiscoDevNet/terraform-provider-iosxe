---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iosxe_device_tracking_policy Resource - iosxe"
subcategory: "System"
description: |-
  This resource can manage the Device Tracking Policy configuration.
---

# iosxe_device_tracking_policy (Resource)

This resource can manage the Device Tracking Policy configuration.

## Example Usage

```terraform
resource "iosxe_device_tracking_policy" "example" {
  name         = "DT_trunk_policy"
  trusted_port = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)

### Optional

- `delete_mode` (String) Configure behavior when deleting/destroying the resource. Either delete the entire object (YANG container) being managed, or only delete the individual resource attributes configured explicitly and leave everything else as-is. Default value is `all`.
  - Choices: `all`, `attributes`
- `device` (String) A device name from the provider configuration.
- `device_role` (String) The role of the device attached to the port
  - Choices: `node`, `router`, `switch`
- `device_role_node_legacy` (Boolean)
- `device_role_router_legacy` (Boolean)
- `device_role_switch_legacy` (Boolean)
- `trusted_port` (Boolean) setup trusted port

### Read-Only

- `id` (String) The path of the object.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import iosxe_device_tracking_policy.example "<name>"
```
