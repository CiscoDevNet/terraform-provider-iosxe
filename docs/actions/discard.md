---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iosxe_discard Action - terraform-provider-iosxe"
subcategory: "General"
description: |-
  This action is used to discard the candidate config, reverting it to the running config. The candidate datastore is shared and persists on the device, so changes left staged by a failed commit are re-attempted by every subsequent commit until they are discarded.
---

# iosxe_discard (Action)

This action is used to discard the candidate config, reverting it to the running config. The candidate datastore is shared and persists on the device, so changes left staged by a failed commit are re-attempted by every subsequent commit until they are discarded.

## Example Usage

```terraform
action "iosxe_discard" "example" {
  config {
    device = "router1"
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Optional

- `device` (String) A device name from the provider configuration.
